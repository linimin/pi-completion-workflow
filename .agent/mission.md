# Mission

Project: pi-letscook

Mission anchor:
Redesign `/cook` startup so it first generates an operator-confirmable startup brief from recent chat, then passes that brief into the completion workflow as advisory intake instead of relying on manual `/cook <hint>` input.

This file is a tracked human-readable statement of the repo's completion mission. Re-grounders may refine this file when repo truth becomes clearer, but it must stay truthful to shipped behavior and the active completion objective.
